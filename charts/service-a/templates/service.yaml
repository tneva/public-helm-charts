apiVersion: v1
kind: Service
metadata:
  name: {{ template "helm-servicea.fullname" . }}
  labels:
    app: {{ template "helm-servicea.name" . }}
    chart: {{ template "helm-servicea.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "helm-servicea.name" . }}
    release: {{ .Release.Name }}